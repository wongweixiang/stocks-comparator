{
  "name": "stocks-comparator",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev:frontend\" \"npm:dev:backend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "tsx backend/index.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "ts-node": "^11.0.0"
  }
}
